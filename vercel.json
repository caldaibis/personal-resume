{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "astro",
  "buildCommand": "npx playwright install chromium && npm run build:pdf && npm run build",
  "outputDirectory": "dist"
}
